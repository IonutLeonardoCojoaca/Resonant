@AGENTS.md

## Entorno Claude Code (Windows)

- Usa `gradlew.bat` (no `gradlew`) al invocar el Gradle Wrapper, tanto desde el Bash tool (Git Bash) como desde PowerShell.
- Cualquiera de los dos tools sirve para los comandos de verificación listados en AGENTS.md.

<?xml version="1.0" encoding="utf-8"?>
<!-- Selected-tab "bubble" for songTabLayout: drawn as the TabLayout indicator
     with app:tabIndicatorGravity="stretch" so it fills (and slides between)
     whole tab cells instead of a thin underline. Inset to leave breathing
     room around the icon. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:insetLeft="6dp"
    android:insetTop="6dp"
    android:insetRight="6dp"
    android:insetBottom="6dp">
    <shape android:shape="rectangle">
        <solid android:color="@color/secondaryColorTheme" />
        <corners android:radius="20dp" />
    </shape>
</inset>

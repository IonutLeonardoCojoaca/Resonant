<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="8dp">

    <!-- Resonant accent top bar (only visible for system sections) -->
    <View
        android:id="@+id/resonantAccentBar"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@color/secondaryColorTheme"
        android:visibility="gone" />

    <!-- Section header: avatar + name + subtitle -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:paddingTop="18dp"
        android:paddingBottom="10dp">

        <!-- Avatar: shows either initials OR Resonant logo -->
        <FrameLayout
            android:layout_width="44dp"
            android:layout_height="44dp">

            <!-- User initials circle (default) -->
            <FrameLayout
                android:id="@+id/initialsContainer"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="@drawable/bg_accent_pill">

                <TextView
                    android:id="@+id/tvOwnerInitial"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="U"
                    android:textColor="@color/white"
                    android:textSize="18sp"
                    android:fontFamily="@font/unageo_bold" />
            </FrameLayout>

            <!-- Resonant logo (only for system sections) -->
            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/ivResonantAvatar"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:src="@drawable/image_resonant_logo_v2"
                android:scaleType="centerCrop"
                android:visibility="gone"
                app:shapeAppearanceOverlay="@style/CircularUserImage" />

        </FrameLayout>

        <!-- Name + subtitle column -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="12dp">

            <!-- Name row with optional "OFICIAL" badge -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tvSectionOwnerName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Usuario"
                    android:textColor="@color/white"
                    android:textSize="16sp"
                    android:fontFamily="@font/unageo_bold" />

                <TextView
                    android:id="@+id/tvOfficialBadge"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:text="OFICIAL"
                    android:textColor="@color/secondaryColorTheme"
                    android:textSize="8sp"
                    android:fontFamily="@font/unageo_black"
                    android:letterSpacing="0.08"
                    android:background="@drawable/bg_resonant_badge"
                    android:paddingHorizontal="5dp"
                    android:paddingVertical="2dp"
                    android:visibility="gone" />

            </LinearLayout>

            <TextView
                android:id="@+id/tvSectionSubtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="1 playlist pública"
                android:textColor="#99FFFFFF"
                android:textSize="12sp"
                android:fontFamily="@font/unageo_medium"
                android:layout_marginTop="2dp" />

        </LinearLayout>

    </LinearLayout>

    <!-- Horizontal scroll of playlists for this section -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvSectionPlaylists"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="10dp"
        android:clipToPadding="false"
        android:overScrollMode="never" />

    <!-- Separator -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="16dp"
        android:background="#15FFFFFF" />

</LinearLayout>
